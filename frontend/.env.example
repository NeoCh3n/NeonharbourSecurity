VITE_CLERK_PUBLISHABLE_KEY=pk_test_your_test_key_here
# Optional: point directly to your API (both dev and prod). If unset, app uses same-origin '/api'.
# VITE_API_BASE_URL=http://localhost:3000
# Optional: enable MSW mocks in dev
# VITE_ENABLE_MSW=true
